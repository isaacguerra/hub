<% content_for :title, "Novo Comunicado" %>

<div class="row justify-content-center">
  <div class="col-lg-8">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="h3 text-primary-custom mb-0"><i class="bi bi-megaphone me-2"></i>Novo Comunicado</h1>
      <%= link_to comunicados_path, class: "btn btn-outline-secondary btn-sm" do %>
        <i class="bi bi-arrow-left me-1"></i> Voltar
      <% end %>
    </div>

    <%= render "form", comunicado: @comunicado %>
  </div>
</div>
