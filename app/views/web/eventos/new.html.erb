<% content_for :title, "Novo Evento" %>

<div class="row justify-content-center">
  <div class="col-lg-8">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="h3 text-primary-custom mb-0"><i class="bi bi-calendar-plus me-2"></i>Novo Evento</h1>
      <%= link_to eventos_path, class: "btn btn-outline-secondary btn-sm" do %>
        <i class="bi bi-arrow-left me-1"></i> Voltar
      <% end %>
    </div>

    <%= render "form", evento: @evento %>
  </div>
</div>
