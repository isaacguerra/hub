<% content_for :title, "Novo Convite" %>

<div class="row justify-content-center">
  <div class="col-lg-6">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="h3 text-primary-custom mb-0"><i class="bi bi-envelope-plus me-2"></i>Novo Convite</h1>
      <%= link_to convites_path, class: "btn btn-outline-secondary btn-sm" do %>
        <i class="bi bi-arrow-left me-1"></i> Voltar
      <% end %>
    </div>

    <%= render "form", convite: @convite %>
  </div>
</div>
