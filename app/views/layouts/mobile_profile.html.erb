<!DOCTYPE html>
<html>
  <head>
    <title>Meu Perfil - Appivone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    
    <!-- PWA / Mobile Standalone Config -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Appivone">
    <meta name="theme-color" content="#6366f1">
    
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <%= stylesheet_link_tag "mobile_design_system" %>
    <%= javascript_importmap_tags %>
    
    <style>
      body {
        background-color: #f8f9fa;
        padding-bottom: 0;
      }
    </style>
  </head>
  <body data-controller="mobile-loader">
    <!-- Loader Overlay -->
    <div class="mobile-loader-overlay d-none" data-mobile-loader-target="spinner">
      <div class="spinner-border mobile-loader-spinner" role="status">
        <span class="visually-hidden">Carregando...</span>
      </div>
    </div>

    <!-- No default header, full width content -->
    <%= yield %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
